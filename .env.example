APP_ENV=local
APP_PASSWORD=replace-with-a-long-random-password
# Set to 1 only when the site is strictly local and you intentionally want no login.
APP_ALLOW_NO_AUTH=0
TRUST_PROXY=0

# Optional: alternatively save the key from Settings for authenticated local SQLite storage.
KIT_API_KEY=
